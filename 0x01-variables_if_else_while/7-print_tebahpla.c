#include <stdio.h>
/**
 * main-program entry point.
 * Return: 0 if no errors, non zero if errors
 */
int main(void)
{
	char le;

	for (le = 'z'; le >= 'a'; le--)
		putchar(i);

	putchar('\n');

	return (0);
}
